<%= form_for @userparam, url: userparam_path(current_user.id) do |f| %>

  <% if @userparam.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@userparam.errors.count, "error") %> prohibited this post from being saved:</h2>

    <ul>
    <% @userparam.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  Name: <%= f.text_field :name %><br />
  Phone number: <%= f.number_field :mobnumber %><br />

  <%= f.submit "Update" %>
<% end %>
